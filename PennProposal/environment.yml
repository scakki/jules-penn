name: bolt
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - pip:
    - mujoco>=3.1
    - gymnasium[mujoco]>=1.0
    - stable-baselines3>=2.3
    - wandb
    - tensorboard
